# Agents Labs - Omni Chat

Plataforma de atendimento omnichannel (React, Supabase, Edge Functions).

## Desenvolvimento

```bash
npm install
npm run dev
```

Configure as variáveis em `.env` (Supabase, Meta, etc.) conforme `supabase/functions/secrets.env.example` e documentação interna do projeto.
